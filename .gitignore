.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# vector stores and local db artifacts (regenerable)
.chroma/
data/chroma/
boston.db

# never commit decompressed source data (the .gz files are the checked-in
# copies); data/reference/ is deliberately not covered by these rules
data/*.csv
data/*.json
!data/*.gz

.DS_Store
.cursor/

# working docs and editor config — kept locally, not part of the submission
PRD.md
PRD.pdf
.codex/
